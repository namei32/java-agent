# ADR-0014：Python 退役前必须完成可回退演练

- 状态：已接受
- 日期：2026-07-18
- 阶段：R9

## 决策

生产切换以可重复的 sandbox 演练、备份 Manifest、差异报告和回退 Runbook 为先决条件。应用默认只能生成计划或
在明确 sandbox 中演练；真实写入切换不由配置开关或 CI 自动触发。

## 后果

得到可审计、可回退的迁移路径，避免 Java/Python 双写和不可逆覆盖。代价是实际生产切换仍需独立执行授权，
且 R9 的“代码完成”不表示 Python 已被退役。
